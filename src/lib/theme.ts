/**
 * Cores que precisam existir em TypeScript (metadata do Next não lê CSS var).
 * Espelham os tokens de mesmo nome em src/app/globals.css — mudar lá, mudar aqui.
 * Só o que tem consumidor: cada constante a mais é superfície para dessincronizar.
 */
export const COR_MARCA = "#b7332a";
export const COR_MARCA_TINTA = "#5e1610";
